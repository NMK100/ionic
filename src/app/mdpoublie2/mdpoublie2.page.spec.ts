import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Mdpoublie2Page } from './mdpoublie2.page';

describe('Mdpoublie2Page', () => {
  let component: Mdpoublie2Page;
  let fixture: ComponentFixture<Mdpoublie2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Mdpoublie2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
