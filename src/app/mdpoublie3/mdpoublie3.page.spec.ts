import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Mdpoublie3Page } from './mdpoublie3.page';

describe('Mdpoublie3Page', () => {
  let component: Mdpoublie3Page;
  let fixture: ComponentFixture<Mdpoublie3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Mdpoublie3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
